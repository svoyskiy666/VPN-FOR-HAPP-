# Disabled: private VPS subscription — no public free nodes.
name: Update VPN nodes
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Public free-node aggregation disabled. Using private VPS only."
